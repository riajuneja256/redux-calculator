export const onClickNumber = (Modal) => ({
    type: 'NUMBER',
    Modal,
  });

export const onClickOperator = (Modal) => ({
    type: 'OPERATOR',
    Modal,
  });

export const onClickEqual = (Modal) => ({
    type: 'EQUALTO',
    Modal,
  });